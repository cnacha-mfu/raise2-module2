# ใบงานที่ 1 — ระบบเริ่มมีความจำ (สัปดาห์ที่ 6)

⏱ เสาร์ 13:00–16:00 · ทำกับ 🔧 **LeaveEasy** (ระบบขอลาออนไลน์) บนเครื่องของคุณเอง
📤 **ส่งใบงานนี้:** วาง **URL ของ repo บน GitHub** ใน Google Classroom ภายใน **จันทร์ 31 ส.ค. 2569**

> 🖥️ **กติกาข้อเดียวของคาบ:** ตกลงว่า**รอบนี้ใครนำจอ** · คนนำจอแชร์จอแล้วเล่าไปทำไป · **ถึง Checkpoint แล้วสลับคนนำจอ** · ทุกคนพิมพ์เองบนเครื่องตัวเอง
> 🚦 พิมพ์สถานะในแชท: 🟢 ไปต่อได้ · 🟡 ติดแต่ยังไหว · 🔴 ติดจนหยุดเดิน · 💬 ติดเกิน 10 นาที → เปิด **ธนาคารคำตอบสด** ก่อน แล้วค่อยโพสต์ **คิวถามกลาง**

---

## 🎯 จบใบงานนี้คุณจะมี

1. 📁 **repo LeaveEasy ของคุณเองบน GitHub** ที่มี commit ในชื่อคุณ และเปิดเว็บในเครื่องได้
2. 📝 **โครงสร้างข้อมูลบนกระดาษ 1 แผ่น** — ชื่อโฟลเดอร์ ชื่อไฟล์ ชื่อช่องข้อมูล ครบทุกตัว
3. 🗄️ **Firestore** (คลังเก็บข้อมูลบนคลาวด์) ที่มีใบลาตัวอย่าง 5 ใบ เปิด Console ดูเห็นด้วยตา
4. 🖥️ **หน้ารายการใบลาที่อ่านจากคลังจริง** — แก้ข้อมูลใน Console แล้วหน้าเว็บเปลี่ยนตาม

> ⛔ **วันนี้ทำถึงตัว R (อ่านข้อมูล) เท่านั้น** — ปุ่มเพิ่ม / แก้ / ลบ และล็อกอิน เป็นของสัปดาห์ที่ 7 · ทำล่วงหน้าแล้วสัปดาห์หน้าจะพัง

---

## ✅ ต้องมีก่อนเริ่ม

| ต้องมี | ถ้าไม่มีให้ทำอย่างไร |
|---|---|
| Claude Code เปิดได้ | พิมพ์ `claude` ใน terminal · ไม่ขึ้นให้โพสต์คิวถามกลางทันที |
| Git ติดตั้งแล้ว | ให้ Claude ตรวจให้ — *"ช่วยเช็กว่าเครื่องผมมี Git แล้วหรือยัง"* |
| บัญชี Firebase | เข้า console.firebase.google.com ด้วยบัญชี Google ที่ใช้ในคอร์ส |
| **บัญชี GitHub เข้าได้** | ยังไม่มีให้สมัครฟรีตอนนี้เลย · ⭐ **เปิด github.com ล็อกอินค้างไว้ในเบราว์เซอร์ตั้งแต่ก่อนเข้าคาบ** จะทำให้ขั้น A2 เหลือกดปุ่มเดียว |

📦 **repo ต้นแบบ** `https://github.com/cnacha-mfu/leaveeasy-start` · 👀 ดูหน้าตาก่อนได้ที่ `cnacha-mfu.github.io/raise2-module2/materials/shared/prototypes/leaveeasy/`

> ✅ **ขั้น A1 จะให้คุณกด Fork** ก๊อปปี้ repo ต้นแบบมาไว้ในบัญชีคุณ · **ผู้สอนตรวจงานจาก repo นี้โดยตรง** ดังนั้นทำอะไรสำเร็จให้ commit แล้วส่งขึ้น GitHub ทุกครั้ง
> 🚫 **เฉลยของแต่ละสัปดาห์อยู่คนละ repo** (`cnacha-mfu/leaveeasy` แท็ก `week6-end` ถึง `week9-end`) — เปิดตอนติดจริงเท่านั้น

---

## 🕐 เวลาของคาบนี้

| เวลา | ทำอะไร |
|---|---|
| 13:00–13:10 | เปิดใบงาน · ตกลงว่าใครนำจอคนแรก |
| 13:10–14:20 | **ส่วน A → B** (🚩1 · 🚩2) |
| 14:20–14:30 | ☕ พักบังคับ 10 นาที — ลุกจากหน้าจอจริง ๆ |
| 14:30–15:45 | **ส่วน C → D** (🚩3 · 🚩4) · จากนั้นปิดคาบ |

---

## ส่วน A — LeaveEasy เข้า repo ของคุณ ⏱ ~31 นาที

### A1 — Fork repo ต้นแบบมาเป็นของคุณ แล้วโหลดลงเครื่อง (8 นาที)

**ทำบนเว็บก่อน (2 นาที)** — เข้า `https://github.com/cnacha-mfu/leaveeasy-start` → กดปุ่ม **Fork** มุมขวาบน → เลือกบัญชีของคุณ → **Create fork**

**จะเห็น:** repo ใหม่ชื่อ `<ชื่อบัญชีของคุณ>/leaveeasy-start` อยู่ในบัญชีคุณ ใต้ชื่อมีบรรทัดเล็ก ๆ ว่า `forked from cnacha-mfu/leaveeasy-start`

> 💡 **Fork = ก๊อปปี้ทั้ง repo มาไว้ในบัญชีคุณ** ของต้นแบบไม่ถูกแตะต้อง · 📌 **URL นี้คือสิ่งที่คุณจะส่งเป็นใบงาน** เปิดค้างไว้อีกแท็บ

จากนั้น **เปิด Claude Code ในโฟลเดอร์ที่คุณอยากเก็บงาน** แล้วสั่ง

```prompt
ช่วยโหลด repo ของผมจาก https://github.com/<ชื่อบัญชีของคุณ>/leaveeasy-start
ลงมาไว้ในเครื่อง ในโฟลเดอร์ชื่อ leaveeasy
บอกผมทีละขั้นว่าทำอะไรบ้าง
```

**จะเห็น:** โฟลเดอร์ `leaveeasy` มี `index.html` · `leave-requests.html` · `new-leave-request.html` · `leave-request-detail.html` · โฟลเดอร์ `css/` และ `js/`

> 🆘 Claude บอกว่าเครื่องยังไม่มี Git → ให้มันติดตั้งให้ *"ช่วยติดตั้ง Git ให้ผมทีละขั้น"* · ติดไม่ผ่านให้โพสต์คิวถามกลาง
> 💡 **ขั้นนี้ยังไม่ต้องล็อกอิน GitHub** เพราะ repo เป็นแบบเปิด · การล็อกอินอยู่ในขั้น A2 ถัดไป

### A2 — 🔑 เชื่อมเครื่องคุณกับ GitHub ครั้งเดียวจบ (8 นาที)

> 📌 **ทำขั้นนี้ก่อนแตะโค้ด** เพราะถ้าจะติด จะได้ติดพร้อมกันทั้งห้องตอนที่ผู้สอนกับ TA ช่วยได้ทีเดียว
> 🔐 ทำครั้งนี้ครั้งเดียว **ใช้ได้ตลอด 4 สัปดาห์** ทั้งใบงานและการบ้าน

```prompt
ช่วยเขียนชื่อผมลงในไฟล์ README.md ชื่อ <ชื่อ-นามสกุลคุณ>
แล้ว commit และส่งขึ้น GitHub ที่ repo ของผมให้ด้วย
```

**จะเห็น:** ตอนส่งขึ้นครั้งแรก **หน้าต่างของ GitHub จะเด้งขึ้นมาเอง** → กด **Sign in with your browser** → ล็อกอินด้วยบัญชี GitHub ของคุณ → กด **Authorize**
จากนั้นเปิดหน้า repo บนเว็บ กด Refresh — **เห็นชื่อคุณอยู่ในไฟล์ README.md และมี commit ชื่อคุณอยู่บนสุด**

> ⏳ **ระหว่างหน้าต่างเปิดอยู่ จอของ Claude จะนิ่งเหมือนค้าง — ปกติครับ** มันกำลังรอคุณกด · **มองไม่เห็นหน้าต่าง ให้กด `Alt + Tab`** เพราะมันมักไปซ่อนอยู่หลังหน้าต่างอื่น
> ✅ **ผ่านครั้งนี้ครั้งเดียว แล้วจบ** — ครั้งต่อ ๆ ไป Claude ส่งงานขึ้น GitHub ให้เองเงียบ ๆ ไม่ถามอีกเลยตลอด 4 สัปดาห์ **คุณยังสั่ง git ผ่าน Claude เป็นภาษาไทยได้เหมือนเดิมทุกอย่าง**
> 🚨 **ห้ามพิมพ์รหัสผ่าน GitHub หรือโทเคนลงในช่องแชทของ Claude เด็ดขาด** และห้ามเก็บลงไฟล์ในโปรเจกต์ — หน้าต่างที่เด้งมาเป็นของ **Windows** ไม่ใช่ของ Claude · Windows จำรหัสให้เอง **Claude ไม่ต้องรู้รหัสอะไรทั้งนั้น**
> 🆘 **ไม่มีหน้าต่างเด้งขึ้นมาเลย** → ใช้ทางสำรอง บอก Claude ว่า *"ช่วยติดตั้งเครื่องมือ GitHub CLI แล้วพาผมล็อกอินด้วยวิธีเปิดเบราว์เซอร์ ทีละขั้น"*
> ทางสำรองนี้จะมี**หน้าจอถาม-ตอบที่คุณต้องกดเอง** — ใช้ **ลูกศรขึ้น-ลง แล้ว Enter** ตอบ 4 ข้อ แล้วจะได้ **รหัส 8 ตัว** ไปวางในหน้าเว็บที่เปิดขึ้นมา
> ⚠️ **ติดตั้งเสร็จแล้วยังใช้ไม่ได้ → ปิด terminal แล้วเปิดใหม่** ข้อนี้คนติดกันเยอะ ถือว่าปกติ
> 🆘 **ขึ้นให้ใส่ username/password ใน terminal** → GitHub ไม่รับรหัสผ่านแบบนั้นแล้ว **อย่าพิมพ์ลงไป** กด `Ctrl + C` ยกเลิก แล้วใช้ prompt บรรทัดบน
> 🆘 **ชื่อผู้ทำ commit ไม่ใช่ชื่อคุณ** → พิมพ์บอก *"ชื่อผู้ทำ commit ยังไม่ใช่ชื่อผม ช่วยตั้งให้หน่อย"* — ข้อนี้สำคัญ เพราะเกณฑ์ผ่านคือ commit ในชื่อคุณ

### A3 — ให้ Claude รันเว็บให้ดูในเบราว์เซอร์ (10 นาที)

```prompt
นี่คือเว็บ LeaveEasy ระบบขอลา เป็น HTML ธรรมดา
ช่วยบอกวิธีเปิดดูในเบราว์เซอร์ให้ผมทีละขั้น อย่าเพิ่งแก้ไฟล์อะไร
```

**จะเห็น:** Claude บอกคำสั่งเปิดเซิร์ฟเวอร์ในเครื่อง · เปิดเบราว์เซอร์แล้วเห็นหน้าแรก LeaveEasy พร้อมลิงก์ไปหน้ารายการใบลา

> 🆘 หน้าขาว → กด F12 เปิดแท็บ Console แล้วคัดลอกข้อความสีแดงไปถามในคิวถามกลาง · 💡 **commit คือปุ่มเซฟที่ย้อนกลับได้** ทำอะไรสำเร็จให้ commit ทุกครั้ง

### A4 — ลองพิสูจน์ว่าเว็บนี้ "ยังไม่มีความจำ" (5 นาที)

เปิดหน้ายื่นใบลา กรอกอะไรก็ได้ กดบันทึก แล้ว **กด F5 รีเฟรช**

**จะเห็น:** ข้อมูลที่เพิ่งกรอกหายหมด — นี่คือปัญหาที่วันนี้จะแก้

→ 🚩 **Checkpoint 1: หน้า repo บน GitHub มีชื่อคุณใน README + commit ในชื่อคุณ + เว็บเปิดในเบราว์เซอร์ได้**
📸 โพสต์ screenshot จากเครื่องคุณ · 🔁 สลับคนนำจอ

---

## ส่วน B — เขียนโครงสร้างข้อมูลลงกระดาษ ⏱ ~25 นาที

> ✍️ **ส่วนนี้ไม่ใช้คอมพิวเตอร์** — ใช้กระดาษกับปากกา เพราะการวาดด้วยมือทำให้เห็นว่าอะไรขาด

### B1 — เขียนกติกา 4 ข้อไว้หัวกระดาษ (5 นาที)

1. 📁 **Collection = โฟลเดอร์** · 📄 **Document = ไฟล์** · ✏️ **Field = ช่องข้อมูลในไฟล์**
2. **ไม่มี JOIN** จึงต้อง **จดชื่อซ้ำไว้ในไฟล์เลย** (denormalize) เช่นเก็บ `requesterName` ไว้ในใบลาตรง ๆ
3. ของที่เป็นของใบลาใบนั้นโดยเฉพาะ ให้เป็น **โฟลเดอร์ย่อย (subcollection)** เช่นความเห็นการอนุมัติ

### B2 — วาดโฟลเดอร์ 3 + โฟลเดอร์ย่อย 1 (12 นาที)

วาดตามนี้ แล้ว **เขียนชื่อช่องข้อมูลให้ครบทุกช่อง**

```
📁 users            → name · email · role
📁 leaveTypes       → name
📁 leaveRequests    → title · reason · startDate · endDate · status
                      requesterId · requesterName · approverId · approverName
                      leaveTypeId · leaveTypeName · createdAt
   └ 📁 approvals   → authorId · authorName · message · createdAt
```

**วงกลมสีแดง** รอบช่องที่เป็นชื่อจดซ้ำ (`requesterName` · `approverName` · `leaveTypeName` · `authorName`) แล้วเขียนข้าง ๆ ว่า *"จดซ้ำเพราะไม่มี JOIN"*

### B3 — ตอบคำถาม 2 ข้อลงกระดาษ (8 นาที)

**①** ถ้าสมชายเปลี่ยนชื่อ ต้องไปแก้กี่ที่? **②** ทำไม `approvals` จึงเป็นโฟลเดอร์ย่อย ไม่ใช่โฟลเดอร์แยก?

**จะเห็น:** กระดาษของคุณมีโฟลเดอร์ 3 + โฟลเดอร์ย่อย 1 · ช่องข้อมูลครบ · วงแดงครบ 4 จุด · คำตอบ 2 ข้อ

→ 🚩 **Checkpoint 2: กระดาษเสร็จ ชื่อช่องครบ วงแดงครบ**
📸 ถ่ายรูปกระดาษโพสต์ในแชท · 🔁 สลับคนนำจอ

---

## ส่วน C — สร้าง Firestore แล้วใส่ข้อมูลตัวอย่าง ⏱ ~45 นาที

### C1 — เปิดคลัง Firestore ในโหมดทดสอบ (13 นาที)

เข้า Firebase Console → สร้างโปรเจกต์ชื่อ `leaveeasy-<ชื่อคุณ>` → เมนู **Firestore Database** → **Create database** → เลือก **Test mode** → เลือกที่ตั้ง `asia-southeast1` *(ชื่อเมนูอาจเปลี่ยนได้ — ถ้าหน้าจอไม่ตรงตามนี้ ให้ถาม Claude ว่าปุ่มไหนแทน)*

**จะเห็น:** หน้าจอ Firestore ว่างเปล่า มีปุ่ม **Start collection** ตรงกลาง

> ⚠️ **โหมดทดสอบเปิดให้ใครก็อ่านเขียนได้ และหมดอายุใน 30 วัน** (สัปดาห์ที่ 7–8 จะปิดให้) — วันนี้จึง **ห้ามใส่ข้อมูลจริงของคนอื่น** ใช้ชื่อสมมติ สมชาย สมหญิง สมศรี เท่านั้น

### C2 — เชื่อมเว็บของคุณกับคลัง (12 นาที)

ใน Firebase Console กด ⚙️ → Project settings → เลื่อนลงหา **Your apps** → กดไอคอน `</>` → ตั้งชื่อแอป → **คัดลอกกล่อง `firebaseConfig`**

```prompt
นี่คือค่า firebaseConfig ของผม <วางที่คัดลอกมา>
ช่วยเขียนแผนว่าจะเชื่อมเว็บ LeaveEasy กับ Firestore อย่างไร ขอดูแผนก่อน อย่าเพิ่งลงมือ
```

อ่านแผนที่ได้ ถ้าข้อไหนไม่ตรงให้บอกกลับ เช่น *"ข้อ 2 ผมอยากให้ค่าตั้งค่าอยู่ไฟล์แยก"* แล้วค่อยสั่งลงมือ

**จะเห็น:** มีไฟล์ตั้งค่า Firebase เพิ่มในโฟลเดอร์ `js/` · เปิดเว็บแล้ว Console (F12) ไม่มีข้อความสีแดง

> 🆘 ขึ้น `Firebase: Error (auth/invalid-api-key)` → คัดลอกค่ามาไม่ครบ ให้คัดลอกใหม่ทั้งกล่อง

### C3 — ใส่ข้อมูลตัวอย่าง 5 ใบ (15 นาที)

> ⚠️ **prompt ข้างล่างยาวกว่าปกติโดยตั้งใจ** เพราะเป็น**รายการชื่อช่องข้อมูล** ที่สะกดผิดแล้วระบบจะพังแบบเงียบ ๆ — Firestore แยกตัวพิมพ์เล็กใหญ่

```prompt
ช่วยเขียนสคริปต์ใส่ข้อมูลตัวอย่างลง Firestore ให้ผม ตามนี้
- users 3 ไฟล์: u001 สมชาย ใจดี employee, u002 สมหญิง รักงาน manager, u003 สมศรี ตั้งใจ hr (ช่อง name, email, role)
- leaveTypes 3 ไฟล์: lt001 ลาพักร้อน, lt002 ลาป่วย, lt003 ลากิจ (ช่อง name)
- leaveRequests 5 ไฟล์ ช่อง title, reason, startDate, endDate, status, requesterId, requesterName, approverId, approverName, leaveTypeId, leaveTypeName, createdAt
  ให้ 3 ใบเป็น รอพิจารณา 1 ใบ อนุมัติ 1 ใบ ไม่อนุมัติ
เขียนแผนให้ผมดูก่อน แล้วรอผมสั่งค่อยลงมือ
```

**จะเห็น:** สคริปต์รันแล้วขึ้นข้อความว่าใส่ข้อมูลสำเร็จ

### C4 — เปิด Console ส่องด้วยตา (5 นาที)

กลับไป Firebase Console → Firestore Database

**จะเห็น:** โฟลเดอร์ `users` · `leaveTypes` · `leaveRequests` · กดเข้า `leaveRequests` เห็นไฟล์ 5 ไฟล์ · กดเข้าไฟล์หนึ่งเห็นช่องข้อมูลตรงกับกระดาษของคุณ

> 🆘 ไม่เห็นข้อมูล → กด Refresh หน้า Console ก่อน · ยังไม่มาให้ดู Console ของเบราว์เซอร์ (F12) ว่ามีข้อความสีแดงหรือไม่

→ 🚩 **Checkpoint 3: Firebase Console เห็นใบลา 5 ใบ**
📸 โพสต์ screenshot หน้า Console จากเครื่องคุณ · 🔁 สลับคนนำจอ

---

## ส่วน D — หน้ารายการอ่านจากคลังจริง ⏱ ~30 นาที

### D1 — ต่อหน้ารายการเข้ากับ Firestore (15 นาที)

```prompt
ตอนนี้หน้า leave-requests.html ยังใช้ข้อมูลสมมติในไฟล์อยู่
ผมอยากให้มันอ่านจากโฟลเดอร์ leaveRequests ใน Firestore แทน
ขอดูแผนก่อน อย่าเพิ่งลงมือ
```

อ่านแผน ตรวจว่ามีข้อ *"เรียงจากใหม่ไปเก่า"* หรือยัง ถ้าไม่มีให้บอกเพิ่ม แล้วค่อยสั่งลงมือทีละข้อ

**จะเห็น:** หน้ารายการแสดงใบลา 5 แถว ตรงกับที่เห็นใน Console

> 🆘 หน้าว่างเปล่า แต่ Console ของเบราว์เซอร์ไม่มีสีแดง → มักสะกดชื่อโฟลเดอร์ผิด ตรวจว่าเป็น `leaveRequests` ตัว R ใหญ่

### D2 — พิสูจน์ว่าระบบมีความจำจริง (10 นาที)

เปิด Firebase Console แก้ `title` ของใบลาใบหนึ่ง → กลับมาที่หน้าเว็บ กด F5

**จะเห็น:** ข้อความบนหน้าเว็บเปลี่ยนตามที่แก้ใน Console — แปลว่าหน้าเว็บอ่านจากฐานข้อมูลจริงแล้ว
จากนั้นปิดเบราว์เซอร์ เปิดใหม่ ข้อมูลยังอยู่ครบ

### D3 — เก็บงาน แล้วส่งขึ้น GitHub (5 นาที)

สั่ง *"ปักหมุดเก็บงานวันนี้ แล้วส่งขึ้น GitHub ให้ผม ตั้งข้อความให้สื่อว่าทำอะไรไป"*

**จะเห็น:** เปิดหน้า repo บนเว็บแล้วเห็น commit อย่างน้อย 3 รายการ **ชื่อคุณทั้งหมด** และไฟล์ครบ

→ 🚩 **Checkpoint 4: หน้ารายการอ่านจาก Firestore จริง + แก้ใน Console แล้วหน้าเว็บเปลี่ยนตาม**
📸 โพสต์ screenshot คู่กัน (หน้าเว็บ + Console) 🎥 มีคลิปสั้น 2 นาทีของขั้นนี้ในธนาคารคำตอบสด

---

> 🧠 **AI เริ่มหลงเมื่อไร ใช้ 4 คำสั่งนี้:** `Esc` 2 ครั้ง = หยุดและย้อน · `/compact` = สรุปเรื่องเก่าได้ที่ว่างคืน · `/clear` = ล้างความจำเริ่มใหม่ (**ขอให้สรุปสถานะงานก่อนล้างเสมอ**) · `/usage` = ดูมิเตอร์

---

## 🆘 ติดตรงนี้ให้ทำอย่างไร

| อาการ | ทำอย่างไร |
|---|---|
| `permission denied` ตอนอ่าน Firestore | ตรวจว่าเลือก **Test mode** ตอนสร้างฐาน ถ้าไม่ใช่ให้ไปแท็บ Rules แล้วสร้างใหม่ |
| หน้าเว็บขาว | F12 → แท็บ Console → คัดลอกข้อความสีแดงไปถามในคิวถามกลาง |
| AI แก้จนพังไปหมด | สั่งว่า *"ช่วยแสดงประวัติ commit แล้วย้อนกลับไปจุดที่ยังใช้งานได้ให้ผม"* |
| ชื่อโฟลเดอร์สะกดผิด | ลบโฟลเดอร์เดิมใน Console แล้วรันสคริปต์ใส่ข้อมูลใหม่ ถูกกว่ามานั่งแก้ทีละไฟล์ |
| **Claude ค้างตอนส่งขึ้น GitHub ครั้งแรก** | กด `Alt + Tab` หาหน้าต่างล็อกอิน GitHub ที่ซ่อนอยู่หลังหน้าต่างอื่น · กดเสร็จแล้ว Claude จะเดินต่อเอง |
| **ส่งขึ้น GitHub ไม่ผ่าน / ไม่มีหน้าต่างล็อกอินเด้ง** | บอก Claude *"ช่วยติดตั้ง GitHub CLI แล้วพาผมล็อกอินด้วยวิธีเปิดเบราว์เซอร์ ทีละขั้น"* → ติดตั้งเสร็จให้ปิด-เปิด terminal ใหม่ · 🚨 **ห้ามพิมพ์รหัสผ่านหรือโทเคนลงในแชท** |
| **ทำไม่ทัน** | ให้ถึง 🚩3 เป็นอย่างน้อย · ส่วน D ทำต่อในการบ้านได้ · ติดหนักให้นัด Consult วันอาทิตย์ |

---

## 🚀 ต่อยอด (ถ้าเสร็จก่อนเวลา)

1. ทำให้หน้ารายการแสดง **สถานะเป็นสีต่างกัน** (รอพิจารณา / อนุมัติ / ไม่อนุมัติ)
2. ทำให้หน้ารายละเอียดใบลา (`leave-request-detail.html`) อ่านจาก Firestore ด้วย · เพิ่มบรรทัด *"ทั้งหมด N ใบ"* บนสุดของหน้ารายการ

---

## 📤 ปิดคาบ + สะพานเข้าการบ้าน

1. 📸 โพสต์ screenshot Checkpoint 4 จากเครื่องคุณลงในแชท
2. 🖼️ **เก็บหลักฐานที่ GitHub มองไม่เห็นเข้า repo** — สั่ง Claude ว่า *"ช่วยสร้างโฟลเดอร์ docs แล้วเก็บรูปที่ผมวางไว้เข้าไป แล้ว commit ส่งขึ้น GitHub ให้ผม"* แล้ววางภาพหน้า Firebase Console ลงไป
3. 📤 **ส่งใบงาน:** วาง **URL repo ของคุณ** (`https://github.com/<ชื่อบัญชีของคุณ>/leaveeasy-start`) ใน Google Classroom ภายใน **จันทร์ 31 ส.ค. 2569**
   > 📌 ส่ง **URL อย่างเดียว** ไม่ต้องแนบไฟล์ — ผู้สอนเปิด GitHub ตรวจเอง ดู commit ว่าเป็นชื่อคุณ ดูไฟล์ว่าครบ
4. ✍️ ตอบในแชท: **"ขั้นตอนที่เพิ่งทำกับ LeaveEasy — ในหัวข้อของคุณคืออะไร?"** *(LeaveEasy มีโฟลเดอร์หลักชื่อ `leaveRequests` — ของคุณชื่ออะไร?)*
5. 🏠 **การบ้านที่ 1 อยู่ที่ [`w6-homework.md`](w6-homework.html)** — ทำ 4 ขั้นเดียวกันนี้ **กับหัวข้อของคุณเองจาก Hackathon** ส่งใน Google Classroom **ภายในศุกร์ 4 ก.ย. 2569**
   > 🎯 **โครงงานคุณไม่เหมือน LeaveEasy ใช่ไหม** — เริ่มการบ้านด้วยการอ่าน [คู่มือเลือกขอบเขตโครงงาน](../shared/homework-scope-guide.html) ก่อน
   > เพื่อเลือกว่าจะหยิบ **ส่วนไหน** ของโครงงานมาทำ · เลือกครั้งเดียวใช้ต่อทั้ง 4 สัปดาห์
